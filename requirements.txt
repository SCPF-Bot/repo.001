# --- Core Orchestration ---
natsort==8.4.0
pillow==11.3.0
requests==2.32.4
google-cloud-vision==3.13.0

# --- OCR Engines ---
pytesseract==0.3.13
manga-ocr==0.1.14
paddleocr==3.0.2
paddlepaddle==3.3.1
# Fixed to 4.10 to avoid NumPy 2.0 conflicts
opencv-python-headless==4.10.0.84 

# --- TTS Engines ---
edge-tts==7.2.8
elevenlabs==2.40.0
# Coqui TTS - Requires librosa >= 0.10.0
TTS==0.22.0
# MeloTTS - We use --no-deps to prevent it from forcing librosa 0.9.1
# This works because MeloTTS is compatible with 0.10.0 in practice.
MeloTTS @ git+https://github.com/myshell-ai/MeloTTS.git@main

# --- Utilities ---
numpy==1.26.4
scipy==1.15.3
# Use 2.6.0 (2.11 does not exist)
torch==2.6.0 --extra-index-url https://download.pytorch.org/whl/cpu
# Explicitly include the newer librosa to satisfy Coqui TTS
librosa==0.10.1
